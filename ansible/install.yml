---
- name: Install packages
  hosts: all
  become: true

  vars:
    packages:
      - podman
      - podman-compose
      - nodejs
      - npm

  tasks:
    - name: Install packages
      ansible.builtin.dnf:
        name: "{{ packages }}"
        state: present
